# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/apps/web/src/env.ts"
# should be updated accordingly.

UPLOADTHING_TOKEN='something-cool'

POSTGRES_URL="postgres://something-cool"
POSTGRES_URL_NON_POOLING="postgre://something-cool"
POSTGRES_USER="something-cool"
POSTGRES_HOST="something-cool"
POSTGRES_PASSWORD="something-cool"
POSTGRES_DATABASE="something-cool"
POSTGRES_URL_NO_SSL="postgres://something-cool"
POSTGRES_PRISMA_URL="postgres://something-coole"

# Better Auth environment variables
BETTER_AUTH_SECRET="something-cool"
BETTER_AUTH_URL="http://something-cool"


GOOGLE_CLIENT_ID="something-cool"
GOOGLE_CLIENT_SECRET="something-cool"


NEXT_PUBLIC_BASE_URL="http://something-cool"

# 用來作crons job 的 secret token
CRON_SECRET="something-cool"
# 不會被刪除資料的email
CLEANUP_OWNER_EMAIL="something@cool.com"

# Web / Vercel collaboration variables
# 共編 room 授權；Vercel 與 Cloudflare Worker 共用同一個 HMAC secret。
COLLAB_JOIN_TOKEN_SECRET="a-32-byte-or-longer-random-secret-value"
# Durable Object gateway origins；server 自行組合 control/socket path。
COLLAB_CONTROL_URL="http://127.0.0.1:8787"
COLLAB_RELAY_URL="ws://127.0.0.1:8787"
# 只授權 Cloudflare cron 觸發 bounded outbox drain；值必須與
# Cloudflare Worker 的 COLLAB_CRON_SECRET 完全相同。
COLLAB_OUTBOX_CRON_SECRET="another-strong-random-string"
# 選用緊急開關；正常情況下不要設定。
# COLLAB_ROOMS_DISABLED="true"

# Cloudflare Worker production settings (documentation only)
# 這些不是 Web env，不要將 production secret 寫進這個檔案。
# 請透過 wrangler secret put 或 Cloudflare Dashboard 設定。
# COLLAB_JOIN_TOKEN_SECRET=<same value as Web/Vercel above>
# COLLAB_CRON_SECRET=<same value as Web/Vercel COLLAB_OUTBOX_CRON_SECRET>
# COLLAB_OUTBOX_DRAIN_URL=https://your-web-origin.example/api/collaboration/control-outbox
# COLLAB_ALLOWED_ORIGINS 是非 secret，維護在 apps/collaboration-do/wrangler.jsonc。

# 共編後端速率限制使用的 Upstash Redis（server-side only）。
UPSTASH_REDIS_REST_URL="https://your-database.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-upstash-redis-rest-token"
